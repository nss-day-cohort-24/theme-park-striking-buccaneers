"use strict";

console.log("What's up SearchTimes.js!!");

let searchTimes = require('./fire');

// requiring from the fire.js to call the times of attractions, and return those attractions in those specified times

function area1ShowTimes(id) {
    console.log("fetching area 1 showtimes");
}

// module.exports